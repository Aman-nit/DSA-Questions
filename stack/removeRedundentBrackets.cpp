#include <iostream>
#include <stack>
#include <string.h>

using namespace std;

// Remove extra bracket that have no meaning
// EX : -(a + b) is fine but((a + b)) is not so we have to remove that extra bracket present itn this expression

int main()
{

    stack<int> s;

    return 0;
}