#include <iostream>
using namespace std;

int main()
{
    int n;
    cout << "Enter a number:- ";
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            /* code */
        }
        for (int k = 0; k < n; k++)
        {
            /* code */
        }
    }

    return 0;
}